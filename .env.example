# ============================================================================
# OpsSight Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
# 
# Quick Start:
#   cp .env.example .env
#   # Edit .env with your values
#   make dev  # or: docker-compose up
# ============================================================================

# ----------------------------------------------------------------------------
# DEPLOYMENT MODE
# ----------------------------------------------------------------------------
# Options: "local" | "docker" | "kubernetes"
# - local: Frontend dev server + Backend dev server (for development)
# - docker: Docker Compose (recommended for home lab)
# - kubernetes: Helm deployment (for production-like setup)
OPSSIGHT_MODE=local

# ----------------------------------------------------------------------------
# FRONTEND CONFIGURATION
# ----------------------------------------------------------------------------
# Vite uses VITE_ prefix for client-side environment variables

# Backend API URL (auto-configured in docker/k8s modes)
VITE_API_URL=http://localhost:8000

# Supabase Configuration (Optional - for cloud auth)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Gemini API Key (Optional - for AI assistant)
# Get from: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=

# ----------------------------------------------------------------------------
# BACKEND CONFIGURATION  
# ----------------------------------------------------------------------------
# FastAPI backend settings

# API Server
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true

# CORS Origins (comma-separated)
API_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database (Supabase PostgreSQL or local)
# For Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
# For local: postgresql://postgres:postgres@localhost:5432/opssight
DATABASE_URL=

# Supabase Service Role Key (for backend operations)
# Get from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_SERVICE_ROLE_KEY=

# ----------------------------------------------------------------------------
# KUBERNETES CONNECTIONS
# ----------------------------------------------------------------------------
# Configure your cluster connections here
# Multiple clusters supported - add KUBECONFIG_* for each

# Default cluster (uses ~/.kube/config if not set)
KUBECONFIG_DEFAULT=

# Named clusters (optional)
# KUBECONFIG_DEV=/path/to/dev-cluster.yaml
# KUBECONFIG_STAGING=/path/to/staging-cluster.yaml
# KUBECONFIG_PROD=/path/to/prod-cluster.yaml

# ----------------------------------------------------------------------------
# INTEGRATIONS (Optional)
# ----------------------------------------------------------------------------

# Prometheus
PROMETHEUS_URL=
# Example: http://prometheus.monitoring.svc.cluster.local:9090

# ArgoCD
ARGOCD_URL=
ARGOCD_TOKEN=
# Example URL: https://argocd.example.com

# GitHub (for pipeline data)
GITHUB_TOKEN=
GITHUB_ORG=
# Get token from: https://github.com/settings/tokens

# Terraform Cloud
TFC_TOKEN=
TFC_ORG=
# Get token from: https://app.terraform.io/app/settings/tokens

# ----------------------------------------------------------------------------
# OBSERVABILITY (Optional)
# ----------------------------------------------------------------------------

# OpenTelemetry
OTEL_EXPORTER_OTLP_ENDPOINT=
# Example: http://jaeger:4317

# Loki (for logs)
LOKI_URL=
# Example: http://loki.monitoring.svc.cluster.local:3100

# ----------------------------------------------------------------------------
# SECURITY
# ----------------------------------------------------------------------------

# JWT Secret (auto-generated if not set)
JWT_SECRET=

# Encryption key for sensitive data (auto-generated if not set)
ENCRYPTION_KEY=
