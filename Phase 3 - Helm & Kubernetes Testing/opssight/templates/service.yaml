apiVersion: v1
kind: Service
metadata:
  name: {{ include "opssight.fullname" . }}
  labels:
    {{- include "opssight.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opssight.selectorLabels" . | nindent 4 }}